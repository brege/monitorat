widgets:
  speedtest-controls-only:
    type: speedtest
    name: "Speedtest: Controls Only"
    show:
      controls: true
      history: false

  wiki-speedtest-controls-only:
    type: wiki
    name: "Config: Controls Only"
    _suppressHeader: true
    mode: rail
    doc: ../../docs/single/features/speedtest-controls.md
