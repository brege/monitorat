**Configuration:**

```yaml
{{file:snippets/12-metrics-nas-1.yaml}}
```

The `remote: nas-1` directive tells the central node to fetch metrics from the nas-1 remote. This is federation in its simplest form: one head node, one remote.
