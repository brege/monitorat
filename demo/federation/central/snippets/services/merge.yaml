widgets:
  services-merge:
    type: services
    name: "Services: Merged"
    federation:
      nodes: [nas-1, nas-2]
      display:
        cards: merge

  wiki-services-merge:
    type: wiki
    name: "Config: Services Merged"
    _suppressHeader: true
    mode: rail
    doc: ../../docs/services/merge.md
