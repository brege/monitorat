widgets:
  reminders-stack:
    type: reminders
    name: "Reminders: Stacked"
    collapsible: true
    federation:
      nodes: [nas-1, nas-2]
      display:
        cards: stack

  wiki-reminders-stack:
    type: wiki
    name: "Config: Reminders Stacked"
    parent: reminders-stack
    _suppressHeader: true
    mode: rail
    doc: ../../docs/reminders/stack.md
