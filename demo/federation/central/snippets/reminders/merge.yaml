widgets:
  reminders-merge:
    type: reminders
    name: "Reminders: Merged"
    collapsible: true
    federation:
      nodes: [nas-1, nas-2]
    features:
      reminders:
        merge: true
    sort_by: due.desc

  wiki-reminders-merge:
    type: wiki
    name: "Config: Reminders Merged"
    parent: reminders-merge
    _suppressHeader: true
    mode: rail
    doc: ../../docs/reminders/merge.md
