widgets:
  speedtest-merge:
    type: speedtest
    name: "Speedtest: Merged"
    collapsible: true
    default: chart
    federation:
      nodes: [nas-1, nas-2]
    chart:
      default_period: 1 month

  wiki-speedtest-merge:
    type: wiki
    name: "Config: Speedtest Merged"
    parent: speedtest-merge
    _suppressHeader: true
    mode: rail
    doc: ../../docs/combinators/speedtest.md
