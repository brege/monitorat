widgets:
  speedtest-merge:
    type: speedtest
    name: "Speedtest: Merged"
    default: chart
    federation:
      nodes: [nas-1, nas-2]
      display:
        chart: merge
        table: merge
        button: stack
    chart:
      default_period: 1 month

  wiki-speedtest-merge:
    type: wiki
    name: "Config: Speedtest Merged"
    _suppressHeader: true
    mode: rail
    doc: ../../docs/speedtest/merge.md
